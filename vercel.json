{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/build",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://trip-planner-hos-api-jvfebtxf.fly.dev/api/:path*"
    }
  ]
}
